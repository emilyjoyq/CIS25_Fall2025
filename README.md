// Midterm Bank Account:
// This program simulates a simple bank account system using C++ classes.
// It allows the user to make deposits, record purchases, check balances,
// and automatically save each transaction (with timestamps) to a file
// named "transactions.txt". 
// The BankAccount class stores the account balance and provides
// methods like deposit(), purchase(), and printBalance() for interaction.
// Each method uses a private log() function to record transactions.
// The main() function tests the program by performing sample deposits
// and purchases, then displaying the final balance on screen.
