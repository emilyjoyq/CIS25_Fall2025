# Inventory Access System

This project demonstrates inheritance and polymorphism using a class hierarchy:
- User (base class)
- Employee (derived class)
- InventoryManager (derived from Employee)

Each class overrides the `accessLevel()` method to display a different permission level.
