#include "InventoryManager.h"

int main() {
    // Create an InventoryManager object
    InventoryManager manager;

    // Demonstrates which accessLevel() method is actually executed
    manager.accessLevel();

    return 0;
}
